---
description: Record architecture decisions — the decision behind recent changes, a specific decision, or a retro-documentation sweep of a repo
---

Invoke the **`adr` skill** and follow it to record architecture decision(s). The skill
is the single source of truth for the workflow, modes (A: a decision taken now; B:
retro-documenting a repo), status rules, and CLI usage — this command only routes you
into it.

Pick the mode from my arguments and the session context; if it is genuinely unclear,
ask me first.

$ARGUMENTS
